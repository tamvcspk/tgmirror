# 06 — Lộ trình & câu hỏi mở

## Trạng thái hiện tại

Cập nhật bởi skill `doc-sync` khi một phase bắt đầu/kết thúc.

- [x] Phase 0 — Scaffold (2026-09-19)
- [x] Phase 1 — Login, channels, tạo kênh (2026-09-19)
- [x] Phase 2 — Copy + state + pause/resume (2026-09-20; đã chạy được trên Telegram thật với kênh cho phép forward, kênh `noforwards` mới thử phía không phải admin, xem "Phase 2 — ghi chú")
- [x] Phase 3 — Filters (2026-09-20; pushdown và cơ chế giữ album nguyên vẹn được kiểm bằng `FakeGateway`, chưa thử trên Telegram thật, xem "Phase 3 — ghi chú")
- [x] Phase 4 — Limiter & flood (2026-09-20; kịch bản flood được kiểm bằng `FakeGateway` và đồng hồ giả, chưa gặp FloodWait thật, xem "Phase 4 — ghi chú")
- [x] Tái thiết luồng job (2026-09-20): `clone` chạy ngay và Ctrl+C dừng, nhật ký lần chạy (`history`) thay cho job, chạy lại cùng cặp là delta, pause tại chỗ + phím `p`/`r`/`q` (xem "Tái thiết luồng job")
- [x] Phase 5 — `retry`, `status` (2026-09-20; kịch bản retry được kiểm bằng `FakeGateway` và SQLite thật, kể cả kill giữa chừng; chưa thử trên Telegram thật, xem "Phase 5 — ghi chú"; delta đã có từ việc tái thiết)
- [x] Phase 6 — Reupload (2026-09-20; kịch bản tải lên lại được kiểm bằng `FakeGateway`, client Telethon giả và SQLite thật; trên Telegram thật người dùng đã xác nhận **video tin đơn được tải lên lại đúng là video** (sau khi sửa lỗi `force_document`); phần còn lại và tiêu chí "clone kênh thử có video lớn" chưa thử, xem "Phase 6 — ghi chú"). **Còn một lỗi mở**: wizard `clone` không hỏi mode tải lên lại, xem "Việc còn lại sau phase 6"
- [x] Phase 6b — Analyze (đếm số tin), tiến độ truyền file, trần cho việc tải trước (2026-09-20; kiểm bằng `FakeGateway`, client Telethon giả và SQLite thật; chưa thử trên Telegram thật, xem "Phase 6b — ghi chú"). **Còn lại của đợt tăng tốc**: bộ lập lịch request (`RequestBudget`), tách đẩy byte khỏi đăng tin + pace theo deadline, đọc lại theo lô, gửi bằng tham chiếu file (chờ spike), đường album mới — xem "Kế hoạch tăng tốc"
- [ ] Phase 7 — TUI, doctor, đóng gói
- [ ] Phase 8 — Group, supergroup, forum topics

## Lộ trình

| Phase | Nội dung | Hoàn thành khi |
|---|---|---|
| 0 | Scaffold: `pyproject.toml` (uv), ruff, pytest, `TelegramGateway` protocol + `FakeGateway`, config/paths | `pytest` chạy được với FakeGateway |
| 1 | `tgmirror login`, `tgmirror channels`, tạo kênh đích mới, wizard chọn nguồn/đích | Đăng nhập thật, liệt kê kênh, tạo được kênh |
| 2 | Strategy A (copy) + SQLite state + pause/resume/stop; album; reconcile pending | Kill giữa chừng rồi resume không trùng/sót (test) |
| 3 | Filters: media/hashtag/regex/date/id/size + server pushdown | Bộ test filter + so sánh pushdown vs full-scan |
| 4 | Limiter AIMD, FloodWait handler, PeerFlood, daily cap, long pause, `flood_log` | Test với FakeGateway kịch bản flood |
| 5 | `tgmirror retry`, `status` (delta đã làm bằng `clone`/`run` chạy lại; `sync`, `sync --all`, `jobs`, `rm` bỏ hẳn vì không còn job) | `retry` thử lại đúng các tin `failed`; `status` cho thấy tiến độ, tốc độ, ETA |
| 6 | Strategy B (reupload) cho admin-owned `noforwards`/biến đổi caption; tải trước (pipeline) thay cho uploader song song (người dùng chọn 2026-09-20); tin đặc thù (poll/quiz/game/invoice) | Clone kênh thử có video lớn |
| 6b | Analyze mặc định (đếm số tin, ghi vào lần chạy), tiến độ từng file đang tải xuống/lên, trần cứng cho việc tải trước; tiếp theo: tăng tốc truyền (kế hoạch bên dưới) | `status` và dòng tiến độ nói "x / tối đa y tin"; file lớn có dòng tiến độ; tải trước không vượt `tmp_budget_mb` |
| 7 | Giao diện Rich (xem tiến độ, chạy/dừng/chạy lại; các phím `p`/`r`/`q` và `RunControl` đã có), `doctor`, đóng gói (pipx/uv tool), tài liệu người dùng | Cài được bằng một lệnh |
| 8 | Nguồn group/supergroup/forum: `ChatKind`, ánh xạ topic (`topic_map`), forward vào topic đích, filter `topic`/`from_user` | Clone thử một forum có nhiều topic, đúng topic và đúng thứ tự trong từng topic |
| 9+ | Đồng bộ edit/delete | Theo nhu cầu |

### Tái thiết luồng job (2026-09-20)

Lý do: `new` chỉ lưu một "job" rồi bắt người dùng gõ thêm `tgmirror run <job>`; "chạy tay" phải nghĩa là gõ một lệnh như mọi lệnh khác (foreground, không chạy nền, không lên lịch, Ctrl+C là dừng), chứ không phải gõ hai lệnh. Người dùng cũng không muốn quản lý job: chỉ cần nhật ký các lần chạy, và chạy lại cùng cặp thì là delta. Các ghi chú Phase 2–4 dưới đây viết theo mô hình job (`new`, `run <job>`, `--refilter`, `sync`); bản hiện hành là mục này và `02-cli-ux.md`, `04-state-checkpoint.md`.

Đã có:

- **`tgmirror clone`** (thay `new`): wizard hoặc cờ, xem trước, **một** câu xác nhận, rồi sao chép ngay. Bỏ `--name`, `--run/--no-run`, `JobExists`.
- **Nhật ký + điểm kiểm tra**: bảng `jobs` tách thành `runs` (nhật ký, người dùng thấy qua `history`) và `mirrors` (con trỏ, filter nhớ, `dst_base_id` của một cặp; người dùng không thấy). Không có migration: dự án chưa phát hành nên schema mới thay thẳng `schema.sql`, DB cũ bị xóa.
- **Chạy lại = delta; filter được nhớ**: không cờ lọc → dùng lại filter và chỉ lấy tin mới; filter khác → quét lại từ đầu bỏ qua tin đã sao chép (thay `--refilter`); `--no-filter` bỏ filter cũ.
- **`run [n]`, `pause`, `stop`** giữ lại nhưng nhắm vào lần chạy (không còn id/tên job); `history [n] [--json]` mới.
- **Pause tại chỗ**: tiến trình vẫn sống và giữ terminal, heartbeat vẫn ghi; `p`/`r`/`q` (phím thường vì VS Code nuốt Ctrl+P/Ctrl+R; `cli/keys.py`), Ctrl+C như cũ. Tất cả cùng đi vào `RunControl` để giao diện Rich sau này cắm vào.

Các lựa chọn khi làm (không phải D1–D9; D5 đổi lý do, xem nhật ký quyết định):

- **Vẫn phải có trạng thái theo cặp**: delta cần con trỏ, crash-safety cần `msg_map` ghi trước và `dst_base_id`; không suy ra được từ nhật ký mà không quét lại cả hai kênh. "Không lưu job" = không có thực thể để quản lý, không phải không có trạng thái.
- **`q` thoát mã 0** (như `stop`), chỉ Ctrl+C thoát 130.
- **Pause có hiệu lực ở ranh giới batch**, không cắt ngang lúc chờ FloodWait (stop thì cắt ngang được).
- **Bộ đếm là của từng lần chạy**: con trỏ và `msg_map` là của cả cặp, nhưng `done/failed/skipped_filter` của `history` chỉ gồm việc lần đó làm.
- **`sync`, `sync --all`, `jobs`, `rm` bỏ khỏi lộ trình** (không còn job để liệt kê hay xóa; `run` chính là `sync`). `retry` và `status` vẫn ở Phase 5.
- **Run bị bỏ rơi** (heartbeat cũ hơn 2 phút) được ghi `failed('interrupted')` khi lần sau chạy cặp đó; `--force-takeover` ghi run cũ là `failed('taken_over')`.

Chưa kiểm chứng (người dùng chạy tay): hai bộ đọc phím thật (`msvcrt` trên Windows Terminal/PowerShell, `termios` trên POSIX) chỉ được kiểm bằng script giả; pause/resume tại chỗ qua terminal thứ hai.

### `--fresh` — làm lại từ đầu (2026-09-20)

Đã chạy một cặp nhưng muốn bắt đầu lại từ đầu (đã dọn đích, hoặc muốn bản sao thứ hai) thì trước đây không có cách: đổi filter vẫn bỏ qua tin đã sao chép, còn `--dst-new` là cặp khác. Người dùng chọn: tên **`--fresh`** (không phải `--new-run`: mọi lần `clone` vốn đã là một run mới), có cả ở `clone` và `run`, và wizard hỏi "Tiếp tục / Làm lại" với cặp đã có tin.

- Quên **chỉ tiến độ** (`msg_map`, con trỏ), giữ filter và nhật ký; `--fresh --no-filter` bỏ cả filter.
- Có hàng rào vì có thể trùng tin: một câu hỏi nêu số tin bị quên; `--yes` đồng ý; không terminal + không `--yes` thì mã 2.
- Con trỏ lùi ở hai chỗ, cả hai trong `Store.start_run` (filter đổi, làm lại).
- Để sau: chế độ kiểm tra/sửa (`--verify`: xem tin nào còn ở đích rồi chỉ sao chép lại tin thiếu).

### Phase 1 — ghi chú

Đã có: `login`/`logout`/`whoami`, `channels` (`--search`, `--writable`, `--json`), `new` (bước 1–2 của wizard: chọn nguồn, chọn hoặc tạo đích), `TelethonGateway`/`TelethonAuth`, `--debug`, UI tiếng Việt/Anh (`TGMIRROR_LANG`). Tất cả test bằng `FakeGateway`/`FakeAuth`/`ScriptedPrompter` và stub client Telethon, không cần mạng.

- **`new` chưa lưu job (lúc đó)**: phase 1 dừng sau khi chốt cặp nguồn/đích (và tạo đích nếu được yêu cầu, luôn có xác nhận hoặc `--yes`). Phase 2 đã nối `plan_endpoints`/`materialize` vào `create_job` và lưu job.
- **Chưa có limiter (lúc đó; phase 4 đã làm cho `run`)**: `core/limiter.py` là phase 4. Các lời gọi phase 1 (`iter_dialogs`, `get_entity`, `CreateChannelRequest`, đăng nhập) là thao tác đơn lẻ do người dùng khởi động; FloodWait/PeerFlood được ánh xạ sang `FloodWait`/`PeerFlood`, in một câu và thoát mã 3, không retry. Luật 1 (mọi lời gọi qua limiter) chỉ thực sự áp dụng từ phase 4; không thêm lời gọi ghi hàng loạt trước đó. Phase 2 thêm lời gọi ghi hàng loạt đầu tiên (copy) nên có limiter tạm thời (ghi chú phase 2).
- **Đã kiểm chứng với Telegram thật (2026-09-19, người dùng chạy tay trên Windows/PowerShell)**: `login`, `channels`, `new --src ... --dst-new ...` (kênh broadcast được tạo, có hỏi xác nhận). Còn chưa thử trên account thật: nguồn/đích supergroup, forum, group thường (nên `can_post`/`is_admin` suy từ entity, spike 4, mới xác nhận cho broadcast). Lưu ý PowerShell: `@name` phải đặt trong dấu nháy.

### Phase 2 — ghi chú

Đã có: `store/` (`Store` trên SQLite: `jobs`, `msg_map`, `flood_log`; migration bằng `PRAGMA user_version`), `engine/` (`planner` → `batcher` → `copy` → `runner`, cùng `reconcile` và `jobs`), `TelethonGateway.iter_messages`/`copy_messages`/`last_message_id`, `core/limiter.py` (tạm thời), các lệnh `new` (lưu job; `--name`, `--mode`, `--batch-size`, `--run/--no-run`), `run` (`--force-takeover`), `pause`, `stop`. Tất cả test bằng `FakeGateway` cùng file SQLite thật trong `tmp_path`, không cần mạng. Tiêu chí "kill giữa chừng rồi resume không trùng/sót" được kiểm bằng `tests/integration/test_runner.py`: runner bị giết ngay sau write-ahead (trước lời gọi copy) hoặc ngay sau copy (trước commit), ở từng batch, rồi resume: mỗi tin xuất hiện đúng một lần ở đích, đúng thứ tự.

Các lựa chọn khi làm (không phải D1–D9):

- **Limiter tạm thời (phase 4 đã thay bằng AIMD đầy đủ)** (`core/limiter.py`): chỉ giãn cách batch (`min_delay` ± `jitter`, nghỉ dài mỗi `long_pause_every` tin). AIMD, `daily_cap`, `limiter_state` là phase 4 sau cùng giao diện `acquire(cost)`.
- **FloodWait chưa được chờ (phase 4 đã chờ tự động; chỉ còn dừng job khi quá `max_auto_wait`)**: batch bị từ chối được xóa khỏi `pending` (Telegram không tạo gì), job thành `waiting_flood` + `resume_at`, ghi `flood_log`, thoát mã 3; `run` từ chối chạy lại trước `resume_at`. Chờ tự động (`max_auto_wait`) là phase 4. PeerFlood: job `failed(peer_flood)`, `run` từ chối trong 24h kể từ lúc đó.
- **Một job cho mỗi cặp nguồn/đích**: `new` từ chối tạo job thứ hai cho cùng cặp (mã 2, chỉ tới job đã có), vì nó sẽ copy mọi thứ hai lần.
- **`run` chạy được với job `done`**: nhặt tin mới sau `cursor_src_id`. Phase 5 chỉ thêm `sync` (và `sync --all`) bọc quanh cùng đường đó.
- **Tin `None` từ `copy_messages`** (id đã bị xóa ở nguồn): sau một lời gọi thành công nghĩa là Telegram không tạo tin nào, nên ghi `failed` với `reason='not_copied'` (không phải "chưa rõ"; chỉ lời gọi bị ngắt giữa chừng mới cần reconcile).
- **Batch bị `PerMessage`** (Telegram từ chối chính các id): xóa `pending` của batch rồi gửi lại từng unit một, để một tin hỏng không kéo theo cả batch. Unit đơn lẻ vẫn lỗi thì ghi `failed` cả unit (cả album).
- **Lời gọi bị ngắt (`Transient`)**: giữ nguyên `pending`, job `failed('transient')`; lần `run` sau reconcile.
- **`last_message_id`**: thêm vào protocol để `new` ghi `options.dst_base_id` (id tin mới nhất của đích lúc tạo job); reconcile chỉ đọc đích sau `max(dst_msg_id đã done, dst_base_id)`, không quét cả kênh đích cũ.
- **Tiến độ** là dòng chữ thường (`ui/progress.py`, không ANSI, tối đa một dòng/5 giây). TUI Rich có phím `p`/`q` là phase 7. Ctrl+C lần một: xong batch hiện tại, lưu, thoát mã 130; lần hai: thoát ngay.
- `MediaKind` thêm `geo`, `contact`, `game`, `invoice` cho khớp danh sách giá trị `media` của `03-filters.md`; loại lạ (dice, ...) tạm xếp vào `document`.

Đã kiểm chứng (2026-09-20, người dùng chạy tay): `new`/`run` copy được từ một kênh cho phép forward. Cũng đã chạy đúng trên tài khoản thật: **album** (forward cả danh sách id giữ nguyên album ở đích, xong spike 2) và **kill giữa chừng rồi resume** (reconcile trên đích thật, không trùng/sót). Nguồn `noforwards` là group mà user không phải admin: `new` từ chối đúng như thiết kế (thông báo `err.source_restricted`, D3, mã 4), tức là `Chat.noforwards`/`Channel.noforwards` được nhận ra ở group. Chưa thử: nguồn `noforwards` mà user là admin (cảnh báo `noforwards_admin`, rồi `run` gặp `ForwardsRestricted` → mã 4 mới chỉ test bằng fake). Còn lại chưa kiểm chứng (người dùng chạy tay):

- `forward_messages` trả `None` cho id đã xóa và ném `MessageIdInvalidError` khi mọi id đều đã xóa: đọc từ mã Telethon 1.45, chưa thử thật.
- `get_input_entity(marked id)` trong `run` dựa vào cache entity trong file session (do `iter_dialogs` của `new`/`channels` ghi). Nếu thiếu, gateway báo `NoPermission` "not accessible": chạy `tgmirror channels` để làm mới.
- Spike 7 (poll, quiz, ... khi forward) vẫn mở: phase 2 chuyển mọi tin, không tiền kiểm theo loại.

### Phase 3 — ghi chú

Đã có: package `filters/` (`model`, `parser`, `matcher`, `pushdown`), `engine/preview.py`, `Skip`/`Batch.skipped` ở planner/batcher, `ServerFilter.until`, `TelethonGateway.iter_messages` với pushdown thật và `src_message` điền `hashtags`/`size`/`duration`/`mime`/`views`, `Store.replace_filters`, cờ lọc của `new` và `run --refilter`, `--preview`, `--no-pushdown`, bước 3 của wizard (`Prompter.checkbox`). Thêm dependency `pyyaml` (file `--filter-file`) và `regex` (timeout chống regex bùng nổ mà `re` không có). Tiêu chí "bộ test filter + so sánh pushdown với quét đầy đủ": `tests/unit/test_filter_*.py` và `tests/integration/test_pushdown_equivalence.py` (kênh ngẫu nhiên có album, 17 filter × 12 kênh; đã thử đột biến: bỏ hoàn thiện album hoặc bỏ lề id thì đỏ), cùng `tests/integration/test_runner_filters.py` (kill/resume, pause trong quãng dài không khớp, `--refilter`).

Các lựa chọn khi làm (không phải D1–D9; chi tiết ở `03-filters.md`):

- **Pushdown không được cắt album**: lề `ALBUM_MARGIN` cho ranh giới id/date; khi đẩy `media`/`search` thì planner hoàn thiện album bằng một lần đọc không lọc. Không đẩy `contains` (search theo từ, matcher theo chuỗi con) và `document`/`sticker`/`webpage`.
- **Ngày → id ở gateway** (`get_messages(offset_date=..., reverse=True, limit=1)`), thêm `ServerFilter.until` để `--until` cũng giảm số tin phải đọc.
- **Cursor và `skipped_filter`**: tin bị loại được cộng vào batch đang phát; batcher phát batch sau mỗi 500 tin bị loại (`FLUSH_AFTER`) nên quãng dài không khớp vẫn lưu tiến độ và nghe được pause/stop. Tin bị server loại (pushdown) không được đếm.
- **`--refilter` là cách duy nhất đổi filter** vì mỗi cặp nguồn/đích chỉ một job và `rm` là phase 5; nó là chỗ duy nhất cursor lùi.
- Sửa một lỗi phát hiện khi viết test: `size` được chuẩn hóa thành số byte trần nhưng số trần bị từ chối khi nạp, nên filter đã lưu không nạp lại được; nay lưu dạng `"<byte>B"`.

Chưa kiểm chứng (người dùng chạy tay): tất cả hành vi thật của Telegram ở spike 3 (`search` + `filter` + `reverse` + `min_id`, `get_messages(offset_date, reverse)`, tokenize hashtag, entity hashtag của tin trong group).

### Phase 4 — ghi chú

Đã có: `core/limiter.py` (AIMD, jitter, nghỉ dài, `daily_cap`, bucket đọc, giảm `batch_size` khi ≥ 3 flood/10 phút; đồng hồ và `sleep` tiêm vào), `engine/flood.py` (`FloodGuard`: `pace`, `write`, `reader`, xử lý FloodWait/SlowMode/PeerFlood, `Interrupted`), `Store.load_limiter_state`/`save_limiter_state` và `commit_batch(limiter=...)`, `DailyCapReached`, `FloodWait.slow_mode`, protocol `MessageReader`, khóa `[limits] read_delay`, cờ `run --wait`. Tiêu chí "test với FakeGateway kịch bản flood": `tests/unit/test_limiter.py` và `tests/integration/test_runner_flood.py` (FloodWait một lần → delay nhân đôi, batch gửi lại đúng một lần, không trùng `msg_map`; quá `max_auto_wait` → `waiting_flood`; PeerFlood; pause/Ctrl+C trong lúc chờ; daily cap qua nửa đêm bằng đồng hồ giả; đọc bị flood giữa chừng, kể cả giữa album). Đã thử đột biến (bỏ vị trí đọc tiếp, bỏ xóa `pending` khi `Interrupted`, bỏ lưu `limiter_state`, bỏ nhân đôi delay, bỏ throttle, bỏ daily cap, bỏ `max_auto_wait`, bỏ giới hạn 5 lần, bỏ giãn cách đọc): test đều đỏ.

Các lựa chọn khi làm (không phải D1–D9; chi tiết ở `05-chong-flood.md`):

- **`FloodGuard` thay vì bọc cả gateway**: runner vẫn gọi `pace` *trước* write-ahead (nên lúc ngủ giãn cách không có `pending`), rồi `write` bọc lời gọi copy; đọc đi qua `reader(gateway)` (một wrapper của `iter_messages`). Ghi và đọc cùng chia sẻ một `Limiter`.
- **Đọc bị cắt bởi FloodWait thì đọc tiếp** từ id của tin cuối đã trao (`min_id = id đó`), vì đọc luôn tăng dần; album bị cắt giữa chừng vẫn nguyên vì planner ghép các tin cùng `grouped_id` liên tiếp.
- **Đọc có bucket riêng** (`read_delay`, 0.5 s, nhân theo `delay/min_delay`); phần tra ngày → id của `--since`/`--until` tính là request đọc thêm. Việc khép kín "phase 4 phải đưa vào limiter" của phase 3.
- **5 FloodWait liền cho cùng một lời gọi thì dừng job** (`MAX_FLOODS_PER_CALL`) thay vì ngủ vô hạn; `--wait` không bỏ giới hạn này.
- **`--wait`** chỉ áp cho FloodWait, không cho daily cap (cap là ngân sách tự đặt: người dùng muốn chạy tiếp thì tăng `daily_cap`).
- **Daily cap**: không gửi batch làm `sent_today` vượt cap, trừ batch đầu tiên của ngày; job `waiting_flood` với `fail_reason='daily_cap'` (cột `fail_reason` dùng cho lý do nghỉ); ngày tính theo giờ máy; bộ đếm theo account.
- **SlowMode** giữ nguyên xử lý như FloodWait nhưng ghi `flood_log.kind='slow_mode'` (`FloodWait.slow_mode`).
- **Chưa làm, để phase 7**: đếm ngược động khi chờ (hiện một dòng thông báo); `status` hiển thị delay/flood 24h (phase 5).
- **Không qua limiter** (một lần, do người dùng khởi động): các lệnh phase 1, `last_message_id` khi tạo job, `--preview`. FloodWait ở đó vẫn in một câu và thoát mã 3.

Chưa kiểm chứng (người dùng chạy tay): mọi hành vi thật của Telegram. Chạy một job đủ lớn để chạm FloodWait thật, rồi xem `flood_log` (spike 6) trước khi tin vào các số mặc định; kiểm tra thêm rằng khi `FloodWait` ném ở giữa `iter_messages` thật thì Telethon không để lại trạng thái lạ và lần đọc lại từ `min_id` cho đúng tin kế tiếp.

### Phase 5 — ghi chú

Đã có: `tgmirror retry [n]` và `tgmirror status [--json]`; `get_messages(src, ids)` ở `MessageReader`/`TelegramGateway` (`TelethonGateway`, `FloodGuard.reader`, `FakeGateway`); `planner.failed_units`/`Gone`; `Runner._failed_units` (nguồn `Unit` của lần chạy có `options.retry_of`); `engine/status.py` (`estimate`, `build_report`); `RunOptions.src_last_id`/`retry_of`/`for_pair`; `Store.count_failed`, `mark_gone`, `flood_count_since`; gợi ý `retry` khi một lần chạy kết thúc mà có tin lỗi. Không đổi schema (hai khóa mới nằm trong `options_json`, đã có sẵn chỗ cho khóa lạ). Tiêu chí "`retry` thử lại đúng các tin `failed`; `status` cho thấy tiến độ, tốc độ, ETA": `tests/integration/test_runner_retry.py` (đúng các tin lỗi, không quét nguồn, không đụng con trỏ/filter, album nguyên vẹn kể cả vắt qua ranh giới lô 100 id, tin đã xóa, kill trước/sau copy, FloodWait quá dài, stop, FloodWait lúc đọc theo id), `tests/unit/test_status.py`, `tests/unit/test_cli_retry.py`. Đã thử đột biến (bỏ giữ album qua lô, đổi công thức ETA, áp filter cho retry, không đặt tin đã xóa sang `skipped`, để khóa của lần chạy lọt vào mirror, gợi ý chạy tiếp bằng `run`, bỏ giãn cách đọc theo id, bỏ `run_id` ở `finish_batch`, bỏ khôi phục hàng `failed` khi hủy `pending`): test đều đỏ.

Các lựa chọn khi làm (không phải D1–D9; chi tiết ở `02-cli-ux.md` và `04-state-checkpoint.md`):

- **`retry` là một lần chạy riêng** (dòng trong `history`, pause/stop được, cùng `FloodGuard`/limiter), không phải một chế độ của `run`; đánh dấu bằng `options.retry_of` chứ không thêm cột (không cần migration: dự án chưa phát hành nhưng DB thật đã có dữ liệu clone, xóa nó chỉ để thêm một cờ là quá đắt).
- **`retry n` = các tin `failed` có `run_id = n`** (chính là danh sách `history n`); tin lại lỗi chuyển sang lần retry mới, nên `tgmirror retry` (không số) thử tiếp. Không có `--all` (mọi tin lỗi của cặp): chưa ai cần, thêm sau nếu thiếu.
- **Tin đã bị xóa ở nguồn** → `skipped` + `gone_from_source` thay vì để `failed` mãi (không ai sửa được, và `status` sẽ luôn báo lỗi).
- **`status` chỉ đọc DB**: clone đang chạy giữ session nên không kết nối được từ terminal thứ hai. Vì vậy tổng cho tiến độ/ETA là id tin mới nhất của nguồn, ghi một lần khi lần chạy bắt đầu (`begin_run` thêm một `last_message_id(src)`, thuộc ngoại lệ "chuẩn bị" của luật 1). Tiến độ theo id là ước lượng thô (id có khoảng trống, filter nhảy nhanh); của retry thì chính xác. Tốc độ là trung bình từ lúc bắt đầu, ETA chỉ khi đang `running`.
- **`status` không có số `n`**: chỉ xem lần đang chạy (hoặc gần nhất); chi tiết một lần cũ đã có ở `history n`.
- **`get_messages` tối đa 100 id/lời gọi, phía gọi tự chia** (giống `copy_messages`); `FloodGuard` tính một request đọc cho mỗi lô.

Lỗi phát hiện khi viết test (đã sửa): `insert_pending` ghi đè hàng `failed` thành `pending` và mọi đường hủy `pending` (reconcile thấy chưa gửi, FloodWait quá dài, stop khi chờ) **xóa** hàng đó. Với lần chạy thường không sao (con trỏ đưa tin trở lại), nhưng tin lỗi nằm dưới con trỏ nên một retry bị ngắt sẽ làm mất dấu tin đó hoàn toàn: không được sao chép, cũng không còn ghi nhận là lỗi. Nay hàng `pending` từng lỗi giữ `reason`/`run_id` cũ và quay về `failed` khi bị hủy (`04-state-checkpoint.md`, "Hủy `pending`"); `run_id` chuyển sang lần chạy mới khi batch kết thúc.

Chưa kiểm chứng (người dùng chạy tay): `get_messages(ids=[...])` của Telethon với kênh thật (đọc mã 1.45: một slot mỗi id, `None`/`MessageEmpty` cho tin đã xóa, tự chia 100 id nhưng ta chia trước); forward một danh sách id thưa (album mà chỉ một số thành viên lỗi) có giữ nhóm album ở đích không; ETA trên kênh lớn có đủ tốt để tin.

### Phase 6 — ghi chú

Đã có: `engine/strategy.py` (`Strategy`, `router`), `engine/reupload.py` (`plan_unit`, `send_unit`, `Window`, `Pipeline`, `UnsupportedMedia`), `Batch.strategy` (batcher cắt batch khi chiến lược đổi; unit tải lên lại luôn một batch riêng, phát ngay), protocol gateway `prepare`/`send_prepared`/`send_text` thay cho `reupload` (`TelethonGateway`, `FakeGateway`, `FloodGuard.reader(...).prepare`), viết lại caption (`rewrite_caption`, `strip_source_links`, UTF-16), `SrcMessage.quiz_unanswered`/`title`, `MessageResult.skipped` (+ `stats.skipped_unsupported`), năm khóa mới trong `RunOptions` (`caption`, `caption_text`, `reset_polls`, `ignore_unsupported`, `placeholder`) và `protected_ack`, cờ `clone --mode reupload`, `--caption`, `--caption-text`, `--reset-polls`, `--ignore-unsupported`, `--placeholder`, `--yes-i-administer-this-channel`, bước 4 của wizard, `[limits] prefetch`/`tmp_budget_mb` (thay `upload_concurrency`), dependency `hachoir`, và `begin_run` đọc lại nguồn trước lần chạy có thể tải lên lại. Tiêu chí "clone kênh thử có video lớn": chưa thử (cần Telegram thật). Bằng chứng thay thế: `tests/integration/test_runner_reupload.py`, `tests/unit/test_reupload.py`, `tests/unit/test_telethon_reupload.py`, `tests/unit/test_cli_reupload.py`.

Người dùng chọn phạm vi trước khi làm: đủ theo lộ trình (lõi + tin đặc thù + `--caption`), và "uploader song song" là **pipeline tải trước** (tải unit kế trong lúc tải lên unit hiện tại), không phải nhiều kết nối kiểu FastTelethon. Các lựa chọn khi làm (không phải D1–D9; chi tiết ở `01-kien-truc.md`, `02-cli-ux.md`, `04-state-checkpoint.md`):

- **Mỗi unit tải lên lại là một batch.** Một crash chỉ để lại đúng một unit `pending` nên reconcile so được; album vẫn nguyên. Giá là mỗi unit một lần `pace` (giãn cách 2 giây như mọi lần ghi): với ảnh nhỏ thì chậm hơn chiến lược A rất nhiều, chấp nhận vì reupload vốn dành cho việc mà A không làm được.
- **Chỉ nửa đọc chạy trước** (`prefetch`, mặc định 1, tối đa `prefetch + 1` unit và `tmp_budget_mb` trên đĩa). Việc gửi vẫn tuần tự, đúng thứ tự (D4), một lời gọi một lúc. Lần chạy chỉ forward không đi qua pipeline (đọc trực tiếp như trước).
- **Chưa có delay theo dung lượng** dù `05-chong-flood.md` từng hứa: không có số liệu để chọn hệ số. Sẽ xét khi có `flood_log` thật (spike 6).
- **Tin không hỗ trợ: dừng ở tin đầu tiên gặp** khi không có `--ignore-unsupported`/`--placeholder` (thay cho thiết kế cũ "xem trước đếm số tin không hỗ trợ rồi hỏi"): không quét cả nguồn trước, tiến độ được giữ, chạy lại `clone` với cờ thì đi tiếp. Poll/quiz không có `--reset-polls` thì bị bỏ kèm cảnh báo (đúng thiết kế cũ) và không cần cờ nào khác. Delta không nhặt lại tin đã bỏ.
- **`--caption` chỉ đổi caption của tin media**; `append` chỉ cộng vào caption đã có; `--mode copy` + `--caption` khác `keep` là lỗi mã 2 (forward không sửa được); `--mode auto` chỉ tải lên lại những unit có caption. Không dùng `drop_media_captions` của forward: `none` cũng đi đường tải lên lại để có một đường duy nhất.
- **Cờ của chiến lược B chỉ đi với `--mode reupload`** (ở `auto` không có gì để bỏ qua nên cờ vô nghĩa và bị từ chối).
- **D3: xác nhận của user là của riêng user.** `--yes` không thay được `--yes-i-administer-this-channel`; đó là điểm khác với mọi câu hỏi xác nhận khác. (Bản đầu từ chối nguồn cấm lưu nội dung mà tài khoản không phải admin bất kể cờ; **đã đổi cùng ngày**, xem nhật ký quyết định: có cờ thì cho qua, kèm cảnh báo trách nhiệm.) Lỗ hổng phát hiện khi thiết kế: `run`/`retry` không đi qua `plan_endpoints` nên một cặp đã clone bằng reupload sẽ tiếp tục tải nội dung xuống dù nguồn vừa bật cấm lưu (user không phải admin) → `begin_run` đọc lại nguồn (một request chuẩn bị) trước mọi lần chạy có thể tải lên lại.
- **Bước 4 của wizard**: luôn hỏi mode (`auto`/`copy`/`reupload`), rồi (trừ `copy`) một câu có/không mặc định không cho caption và tin không sao chép được, để clone thường chỉ trả lời hai câu nhanh; nguồn cấm lưu nội dung bỏ câu mode (chỉ còn tải lên lại). Lúc đầu mode chỉ nằm sau câu có/không và thiếu `copy`; người dùng yêu cầu đưa ra thành bước riêng (2026-09-20). Mỗi câu hỏi mới làm lệch các test wizard cũ (chúng viết sẵn danh sách câu trả lời và chỉ số `select_labels`): đã cập nhật.
- **Album qua `send_file([...])` cần `hachoir`**: Telethon gửi mọi file trong album với cùng tùy chọn và đọc duration/w/h từ file; tin đơn thì truyền thẳng `attributes` của tệp gốc. Nếu thấy video trong album mất thông tin, đường thay thế là dựng `InputMediaUploadedDocument` từng tin rồi `SendMultiMediaRequest` (mức thấp, chưa làm).
- Placeholder có thể bị đăng trùng một dòng khi crash đúng lúc đăng (reconcile không so được text với game); chấp nhận (ưu tiên "không sót").

Lỗi người dùng gặp khi thử thật (2026-09-20, đã sửa): **video reupload ra thành tệp thay vì video**. Nguyên nhân: gửi `force_document=True` cùng thuộc tính của tệp gốc, nhưng Telethon đổi nó thành `force_file` trong `InputMediaUploadedDocument` nên Telegram hiện mọi thứ như tệp; giả định ban đầu ("thuộc tính giữ nó là video") sai và một test của tôi còn khẳng định chính hành vi sai đó. Nay chỉ tệp vốn là tệp thường mới bị ép; video/GIF/video tròn/nhạc/voice/sticker không, album nhạc/video cũng không, video thường thêm `nosound_video=True`. Bài học: stub client chỉ kiểm được tham số ta truyền, không kiểm được Telegram hiểu nó ra sao; cần thử thật (mục "Chưa kiểm chứng" dưới vẫn đúng cho những chỗ còn lại).

Lỗi phát hiện khi viết test (đã sửa): (1) `stop` trong lúc unit kế đang tải khiến lần chạy kết thúc `done` thay vì `stopped` (chờ bị cắt bằng cách kết thúc luồng thay vì ném `Interrupted`); (2) hủy pipeline giữa lúc `queue.put` làm rò file của unit vừa tải xong và giữ chỗ trong `Window`; (3) khi kiểm tra đột biến, một đột biến làm task tải nền chết mà không trao lỗi khiến consumer chờ hàng đợi **vô hạn** (test không có timeout, script cũng không): `Pipeline` nay chờ cả task nền (`_next`) và báo lỗi/hủy của nó thay vì chờ mãi, thêm timeout cho từng test (`pytest-timeout`, 60 giây) và viết lại `scripts/mutation_check.py` để chạy trên bản sao tạm, có timeout. Kết quả `scripts/mutation_check.py` (người dùng chạy trong terminal VS Code, 2026-09-20): **25/25 đột biến bị phát hiện** (D3, thứ tự và tách batch, dọn file, dừng khi đang tải, task tải chết, caption/UTF-16/`parse_mode`, placeholder). Lần chạy đầu sót một: xóa việc dọn thư mục tạm ở *đầu* lần chạy vẫn xanh vì `finally` cuối lần chạy cũng dọn; test nay kiểm file cũ đã mất ngay lúc `prepare` đầu tiên. Bộ đột biến này chỉ phủ chiến lược B và D3, không phủ code phase 1–5.

Chưa kiểm chứng (người dùng chạy tay, trên kênh thử có video lớn): mọi hành vi thật của Telegram. Cụ thể: tải media của nguồn cấm lưu nội dung bằng tài khoản admin có được không; `send_file(attributes=..., force_document=True)` có ra video/voice/sticker đúng loại thay vì tệp thường; album qua `hachoir`; ảnh bìa video (`thumb=`); `send_message(file=message.media)` cho poll/location/contact/dice; `results.results` có thực sự cho biết quiz đã trả lời; `message.chat.username` có mặt để `strip-links` nhận ra nguồn; tên tệp `download_media` trả về khi truyền đường dẫn `.part`; thời gian chờ và FloodWait khi upload file lớn (spike 6, spike 7, spike 8 vẫn mở).

#### Việc còn lại sau phase 6

- **Lỗi mở: wizard `clone` không hỏi mode tải lên lại** (người dùng báo 2026-09-20, chưa điều tra; để phiên sau). Bước 4 (`wizard.pick_strategy`) được thiết kế để luôn hỏi mode, nhưng chỉ chạy khi `asked` đúng: wizard phải hỏi cả nguồn lẫn đích (không có `--src`, hoặc không có `--dst`/`--dst-new`), không có `--yes`, và chưa có cờ nào của bước này (`--mode`, `--caption`, ...) (`cli/commands/clone.py`). Nếu người dùng chạy `clone` với `--src`/`--dst` (hoặc `--yes`) mà vẫn muốn được hỏi thì bước này bị bỏ qua; nếu chạy `tgmirror clone` trần mà vẫn không thấy câu hỏi thì là lỗi thật. Việc cần làm: tái hiện cả hai cách chạy, rồi quyết định có nên hỏi mode dù đã có `--src`/`--dst` (giống bước filter, vốn cũng bị bỏ qua khi có cờ) hay chỉ sửa tài liệu, và thêm test cho đường wizard đã chạy đúng cách người dùng chạy.
- Các mục "Chưa kiểm chứng" của phase 6 ở trên (trừ video tin đơn): album video qua `hachoir`, nhạc/voice/GIF/sticker, ảnh bìa, `--caption`, poll/location/contact, nguồn cấm lưu nội dung bằng tài khoản không phải admin, FloodWait khi upload lớn.

### Phase 6b — ghi chú

Yêu cầu của người dùng (2026-09-20): cải thiện tốc độ tải xuống/lên và bước analyze, cần cho việc hiện tiến độ ("có bao nhiêu item cần làm" và tiến độ nhỏ của từng file tải xuống/lên). Sau một vòng brainstorm người dùng chốt:

- **Analyze chỉ đếm số tin, chạy mặc định.** Dung lượng biết sau, từng file, lúc thực hiện. Không quét trước, không "scout" nền.
- **Không cần IPC**: mỗi terminal là một process; `status` từ terminal khác chỉ đọc DB (không có tiến độ từng file, chỉ có "x / tối đa y").
- **Nhiều file cùng lúc được, miễn không vượt trần request**: số request dùng theo dung lượng file (nhỏ dùng ít, lớn dùng nhiều); tải xong file nào thì tải lên ngay trong lúc tải file kế. Album dùng chung ngân sách request trong một unit. Trần kết nối mặc định khoảng 4 rồi đo. Không hoãn nhiều kết nối (đảo lại lựa chọn "chỉ pipeline tải trước" của phase 6, xem nhật ký quyết định).
- **Tải nhanh hơn gửi thì không được nổ RAM/đĩa**: cần trần cứng cho việc tải trước.
- Cho phép mở spike (gửi bằng tham chiếu file, tốc độ nhiều request/kết nối, số đếm) trên kênh thử; người dùng chạy tay.

Đã có: `TelegramGateway.count` (Telethon: một `messages.search`; `FakeGateway`; `_GuardedReader.count` không pace), `Runner._analyze` + `Store.set_total` + `RunOptions.total_items` + `Run.handled`/`already_done` + `Batch.already`, thông báo `analyzed`/`cap_days`, `status` tính theo `total_items` (dòng "Cap ngày", khóa JSON `total_items`/`handled`/`left`/`cap_rest_days`), `OnTransfer`/`TransferPhase` ở gateway (`prepare`, `send_prepared`), `engine/transfer.py` (`TransferTracker`, `Transfer`), `Reporter.transfer`, `LineReporter` (dòng "x/y tin" và dòng cho file ≥ 8 MB), `reserve_size`/`UNKNOWN_SIZE`/`Window.grow`, ba script spike (`scripts/spike_count.py`, `spike_reference.py`, `spike_throughput.py`). Chi tiết cơ chế ở `01-kien-truc.md` ("Analyze và tiến độ"), `02-cli-ux.md`, `04-state-checkpoint.md`, `05-chong-flood.md`. Kiểm bằng `tests/integration/test_runner_analysis.py`, `tests/unit/test_transfer.py`, `tests/unit/test_telethon_transfer.py`, các ca mới ở `test_status.py`, `test_reupload.py`, `test_runner_reupload.py`.

Các lựa chọn khi làm (không phải D1–D9):

- **`count` không qua `pace_read`** để không làm đổi nhịp đọc của mọi lần chạy (trước đó lần đọc đầu tiên đi ngay): một lời gọi mở đầu lần chạy, cùng loại với các lần đọc chuẩn bị. FloodWait của nó vẫn qua `FloodGuard`. Hệ quả là phép đếm cộng thêm một request (vài request nếu có ngày) vào mỗi lần chạy.
- **Tổng là cận trên, được chặn bằng khoảng id** (`min(count, src_last_id - min_id)`), vì chưa biết Telegram có áp `min_id`/`max_id` khi tính `count` hay không. Lỗi Telegram ngoài FloodWait/PeerFlood của phép đếm không làm hỏng lần chạy.
- **`already_done`**: tin vượt qua vì cặp đã có (đọc lại sau khi đổi filter) được đếm là đã xử lý, không thì phần trăm đứng yên. Lần chạy đầu và delta không bị ảnh hưởng.
- **Pace không đổi** ở đợt này: `Limiter.acquire` vẫn ngủ cứng `delay × jitter` trước mỗi lần ghi, và write-ahead vẫn trước lời gọi `send_prepared`. Đổi hai chỗ đó là quyết định về crash-safety, chưa làm (xem dưới).
- **Tiến độ album** lấy từ callback của Telethon (đếm theo file, có phần thập phân), đổi ra byte theo tổng dung lượng các file; dưới id tin đầu của album.

Chưa kiểm chứng (người dùng chạy tay): `messages.search` `limit=1` có trả `count` đúng khi có `min_id`/`max_id`/filter không (`spike_count.py`); `progress_callback` của Telethon với file lớn và album (cỡ callback, có chạy trong lúc `send_file` không); mọi số tốc độ.

#### Gửi bằng mã file — đã làm (2026-09-20)

Sau spike 11 (video, tệp, album 6 video 115–490 MB, nguồn không cấm lưu nội dung) tôi làm phạm vi đã đề xuất; người dùng không phản đối và tiếp tục chạy spike. Phạm vi: chỉ `--mode auto` đổi caption trên nguồn không cấm lưu nội dung và unit toàn file; `--mode reupload` không đổi (đó là điều người dùng chọn); nguồn `noforwards` (kể cả đã tuyên bố theo D3) giữ đường tải xuống rồi tải lên vì chưa có số đo (đã đo 2026-09-21: Telegram từ chối, xem "Kết quả hai spike" ở phần Pool request). Có chuỗi lùi (đọc lại một lần, rồi tải xuống rồi tải lên). Cơ chế ở `01-kien-truc.md` ("Gửi bằng mã file"). Đã có: `Strategy.REFERENCE`, `router(..., by_reference=)`, `has_files`, `MessageReader.fetch`, `TelegramGateway.send_by_reference` (Telethon, `FakeGateway`, `_GuardedReader.fetch`), `send_unit_by_reference`, `RunOptions.src_protected` (do `begin_run` ghi), thông báo `reference_fallback`, tests ở `test_runner_reupload.py`, `test_reupload.py`, `test_telethon_reupload.py`. **Chưa thử trên Telegram thật** ngoài spike: caption/entity trên tin gửi bằng mã, ảnh bìa video, `FILE_REFERENCE_EXPIRED` thật (sau bao lâu), nguồn `noforwards`. Thứ tự ghi trong bản kế hoạch dưới đây, bước 4, nay đã xong.

#### Kết quả `scripts/mutation_check.py` (người dùng chạy, 2026-09-20)

26/29 đột biến bị bắt; 3 bị bỏ qua vì mẫu không còn khớp sau khi mã đổi (`check_source` trả về `bool`, batcher cắt theo `is not Strategy.COPY`), không phải vì test thiếu. Đã sửa ba mẫu, thêm 13 đột biến cho phase 6b (D3 với gửi bằng mã file, làm mới tham chiếu, lùi về tải xuống, ngân sách đĩa, số đếm, `already_done`, pool: giảm ngân sách, nuốt FloodWait, hủy worker, file dở) và thêm các file test mới vào danh sách chạy; script được kiểm là mọi mẫu khớp đúng một lần (chưa chạy lại các đột biến mới). Nhắc lại: mỗi lần đổi mã mà script dựa vào, mẫu có thể lệch, nên chạy `--list` và đọc dòng `SKIPPED`.

#### Pool request — đã làm (2026-09-20)

Người dùng yêu cầu làm bước kế sau khi commit `ba576f8`. Số đo spike 12 quyết định thiết kế: tải xuống **một kết nối, nhiều request đang bay**; tải lên **2–3 kết nối**; trần tổng thấp và tự lùi vì 16 request trên 3 kết nối đã bị 429 ở tầng truyền. Đã có: `core/pool.py` (`RequestBudget`, `run_parts`), `TransportPressure`, `TransferSettings` và các hàm `_download_parallel`/`_upload_parallel`/`_request`/`_upload_senders`/`aclose` trong `telethon_gateway.py`, khóa `[limits] max_requests`, `upload_connections`, `pool_min_mb`; kiểm bằng `tests/unit/test_pool.py` (thuần) và `tests/unit/test_telethon_pool.py` (client giả trả `GetFileRequest`/`SaveBigFilePartRequest` từ bộ nhớ). Cơ chế ở `05-chong-flood.md` ("Pool request").

Các lựa chọn khi làm (không phải D1–D9):

- **Không đổi pace, write-ahead hay crash-safety**: phần tải lên vẫn nằm trong `send_prepared` (sau write-ahead và `pace`), chỉ nhanh hơn. Hai việc cần duyệt riêng (đẩy byte tách khỏi đăng tin, pace theo deadline) **chưa làm**.
- **Mặc định bật** (`max_requests = 4` sau lần chạy thật đầu tiên, xem trên); tắt bằng `0`. Chỉ tin đơn là tài liệu/video ≥ 10 MB dùng pool để tải lên (API big-file; album và ảnh giữ `send_file` của Telethon); tải xuống dùng pool cho mọi tài liệu ≥ `pool_min_mb`.
- **Ưu tiên**: tải lên (số 0) trước tải xuống (số 1) khi tranh slot; gateway chưa biết thứ tự unit nên chưa ưu tiên theo unit.
- **FloodWait giữa chừng làm lại file từ đầu** (chưa nhớ part đã xong); file dở luôn bị xóa khi lỗi để không giữ đĩa.
- Dùng API private của Telethon (`_call`, `_borrow_exported_sender`, `_sender`, `_get_dc`, `_connection`): có thể vỡ khi nâng cấp Telethon (test bằng client giả không bắt được).

**Chưa kiểm chứng trên Telegram thật** (người dùng chạy tay, cần một `clone --mode reupload` với video lớn): (1) video đăng từ `InputFileBig` vẫn là video (không thành tệp), có streaming và ảnh bìa (đã kiểm offline: `client._file_to_media(InputFileBig, attributes=[video], mime_type=..., nosound_video=True)` ra `InputMediaUploadedDocument` với `force_file=False` và thuộc tính video, cùng kết quả như khi truyền đường dẫn; còn thiếu là Telegram hiển thị ra sao); (2) tốc độ thật và không có 429 khi tải xuống và tải lên chạy cùng lúc dưới ngân sách 8; (3) tải xuống từ DC khác DC nhà; (4) file gần 2 GB. Chưa làm: ghi phản kháng tầng truyền vào `flood_log`.

#### Nâng cấp Telethon

Telethon được **ghim đúng một phiên bản** (`telethon==1.45.0` trong `pyproject.toml`, `uv.lock`, và `TELETHON_CHECKED` trong `core/telethon_gateway.py`), vì pool request và đường tải lên lại dựa vào phần bên trong mà Telethon không hứa giữ: `TelegramClient._call`, `_borrow_exported_sender`, `_return_exported_sender`, `_sender`, `_get_dc`, `_connection`, `_log`, `_proxy`, `_local_addr`, `_file_to_media`, `MTProtoSender(auth_key, loggers=)`, `InvalidBufferError.code`, cách `send_file` dựng media video từ `InputFileBig`. Các test dùng client giả không bắt được việc chúng đổi, nên `tests/unit/test_telethon_pin.py` bắt thay: nó **đỏ** nếu phiên bản cài đặt khác `TELETHON_CHECKED`, nếu `pyproject.toml` không ghim đúng phiên bản đó, nếu `telethon_gateway.py` chạm vào thêm một thành viên private mới của client (phải thêm vào danh sách trong test), nếu một thành viên private hay chữ ký đã biến mất, và nếu `send_file` từ một `InputFileBig` không còn dựng media video đúng (không ép thành tệp, giữ thuộc tính video và `nosound_video`).

Checklist mỗi lần nâng cấp (làm theo thứ tự, không bỏ bước):

1. Đọc changelog Telethon giữa hai bản, tìm những thay đổi ở danh sách trên, ở `send_file`/`download_media`/`iter_messages`/`forward_messages` và ở xử lý flood.
2. Đổi ghim: `pyproject.toml` (`telethon==<mới>`), chạy `uv lock`, đổi `TELETHON_CHECKED`.
3. `uv run pytest` — `test_telethon_pin.py` chỉ ra chỗ nào của bề mặt private đã đổi; sửa adapter ở `telethon_gateway.py` (và mở rộng test) trước khi làm tiếp.
4. `python scripts/mutation_check.py` (terminal của người dùng): mọi đột biến phải bị bắt.
5. Chạy lại các spike với tài khoản thật: `spike_count.py` (số đếm theo vị trí), `spike_throughput.py download|upload|both` (tốc độ, không 429), `spike_reference.py` (đơn và `--album`).
6. Chạy tay một `clone` nhỏ: `--mode reupload` với video ≥ 10 MB (video vẫn là video, có streaming và ảnh bìa; đó là điều một stub không kiểm được), `--mode auto --caption append`, một album, và một lần Ctrl+C rồi chạy lại.
7. Cập nhật các mục "chưa kiểm chứng" ở đây và trong skill `telethon-engine`; ghi ngày và phiên bản đã kiểm.
8. Nếu pool hỏng mà chưa sửa được: `[limits] max_requests = 0` đưa việc truyền file về cách của Telethon (không dùng phần private nào ngoài `_file_to_media` qua `send_file`).

#### Lần chạy thật đầu tiên với pool và bản sửa (2026-09-20)

Người dùng chạy `clone --media video --mode reupload` trên nguồn cấm lưu (tuyên bố D3), video 19, 84 và 198 MB. Kết quả: tin 2 (18,7 MB) xuống **3,9 MB/s** (chậm: có thể do lần đầu, dựng kết nối, hoặc file ở DC khác); tin 3 (84,4 MB) xuống **29,7 MB/s**; tin 4 (198 MB) bị **`HTTP 429` ở tầng truyền** ngay đầu lúc tải, trong khi tin 3 đang được tải lên, và lần chạy **thất bại với "Lỗi kết nối tới Telegram"** (`Transient`): pool thử lại 5 lần trong khoảng 15 giây rồi bỏ cuộc. Nguyên nhân có khả năng nhất (không chắc): tải xuống và tải lên cùng đi trên **kết nối chính** (spike chỉ dùng kết nối riêng) cộng ngân sách đã tăng lên khoảng 7–8 request đang bay.

Đã sửa: (1) **HTTP 429 không thử lại tại chỗ** mà thành `FloodWait(60, transport=True)` cho `FloodGuard` ghi (`flood_log.kind = 'transport_429'`, số liệu để chỉnh sau), chờ và lặp lại, tối đa 5 lần rồi lần chạy nghỉ `waiting_flood` như mọi flood; (2) **tải xuống và tải lên có kết nối riêng**, không đi chung kết nối chính với các lời gọi đọc và đăng; (3) **tải xuống nhặt lại các part đã có** sau flood hay mất kết nối thay vì làm lại file 200 MB; (4) mặc định `max_requests` 8 → **4**, ngân sách bắt đầu ở 2. Test: `test_pool.py`, `test_telethon_pool.py`, `test_runner_split.py` (mọi đường trên).

Còn mở: vì sao tin 2 chậm; 429 có tái diễn ở ngân sách 4 với kết nối riêng không (bạn chạy lại, xem `flood_log` bằng `tgmirror history <n>` hay trực tiếp); thời gian nghỉ 60 giây là ước đoán, chưa có số đo.

#### Lần chạy thật thứ hai (2026-09-21): video 1,5 GB và 1,9 GB

Sau bản sửa 429 (mặc định `max_requests = 4`, kết nối riêng, nhặt lại part): **không còn 429 và không lỗi**. `clone --hashtag ... --mode reupload` trên nguồn cấm lưu (tuyên bố D3): tin 15097 (1,5 GB) **tải xuống ổn định 25–30 MB/s** suốt 1,5 GB (đúng số spike với 4 request đang bay). **Tải lên chỉ 1–7 MB/s, phần lớn 2–5 MB/s** (khoảng 11 MB/s ở 52 MB đầu rồi tụt), thấp hơn nhiều so với spike (15–25 MB/s, nhưng spike chỉ đẩy 64 MB); cả 1,5 GB lên mất vài phút và là nút cổ chai của cả chu trình. Đăng thành công, lần chạy ghi 2 tin đã sao chép; người dùng dừng ở tin thứ hai (`q`), chạy tiếp bằng `tgmirror run 13`. Chưa biết video có hiện đúng là video không (người dùng chưa báo).

Nguyên nhân tải lên chậm **chưa rõ**, ba giả thuyết: (a) **Telegram hạn tốc độ tải lên bền vững** (mẫu "nhanh lúc đầu rồi tụt" khớp; spike 64 MB không thấy); (b) ngân sách request của ta bị giảm âm thầm bởi các lần phản kháng không phải flood (kết nối đóng, quá 30 giây không trả lời) và chỉ tăng lại một bậc mỗi 32 part sau 30 giây yên tĩnh, nên kẹt gần 1 request (3 MB/s là tốc độ của một request một lúc); (c) hai kết nối riêng cùng DC chậm hơn số đo spike. Cách phân biệt: (1) bản này **in cảnh báo khi ngân sách giảm** (`[cảnh báo] transfer requests in flight: 4 -> 2 (...)`, kèm lý do): thấy dòng đó thì là (b), không thấy thì là (a) hoặc (c); (2) `scripts/spike_throughput.py upload --mb 512 --inflight 4 --connections 2 --repeat 1 --timeline 10` (đẩy dữ liệu rác, không qua tgmirror) in tốc độ theo từng cửa sổ 10 giây: nếu cũng tụt sau vài chục MB thì là Telegram hạn tốc độ và không sửa được từ phía ta.

Ghi nhận thêm: (i) `tmp_budget_mb = 2048` nên tin 1,9 GB **không được tải trước** trong lúc tin 1,5 GB đang tải lên (tổng > 2 GB): chu trình thành tuần tự, tải xuống + tải lên; tăng `tmp_budget_mb` (đĩa cho phép) nếu muốn chồng lên nhau; (ii) hachoir in `[warn] [<NdsFile>] ...` khi Telethon thử đọc thông tin file lớn: chỉ là tiếng ồn, đã tắt (`hachoir.core.config.quiet`); (iii) Telethon in `Server response had invalid buffer` cho lỗi tầng truyền, ta đã xử lý nhưng dòng của Telethon vẫn hiện.

**Lần chạy thứ ba (2026-09-21, lần chạy 15)**: một **album 2 video** (1,2 GB mỗi cái, 2,4 GB) từ nguồn cấm lưu. Tải xuống lại ổn định **28–32 MB/s** cho cả hai file (tuần tự, mỗi file qua pool). **Tải lên album mất khoảng 24 phút, trung bình ~1,7–2,5 MB/s** (dao động 0,8–4,4 MB/s), không có cảnh báo nào của pool. Lý do đơn giản: **album không đi qua pool khi tải lên** (chỉ tin đơn là tài liệu/video ≥ 10 MB mới đẩy trước bằng pool): nó là `send_file([...])` của Telethon, tuần tự từng file, một request một lúc, đúng cách cũ. Số này gần bằng lần chạy 13 (tin đơn 1,5 GB **có** pool: ~3,2 MB/s trung bình) nên **pool tải lên không cho được số spike (15–25 MB/s) trong lần chạy thật**; cùng dạng "khởi đầu nhanh rồi tụt" (giả thuyết Telegram hạn tốc độ tải lên bền vững vẫn mạnh nhất). Chưa có cảnh báo pool nào để loại giả thuyết ngân sách tụt (lần chạy 13 chạy trước khi có cảnh báo; lần 15 không dùng pool tải lên). **Chưa nên** làm đường album tải lên bằng pool (dựng `InputMediaUploadedDocument` + `UploadMediaRequest` + `SendMultiMediaRequest` thủ công, vì Telethon không cho truyền thuộc tính từng tin khi gửi album từ handle) cho tới khi có số `--timeline`: nếu Telegram hạn tốc độ tải lên thì công sức đó chỉ được ~2 lần; nếu không thì là ưu tiên số một. Cách kiểm nhanh hơn cả: thử **gửi bằng mã file** với nguồn cấm lưu (`scripts/spike_reference.py ... --yes-i-administer-this-channel`, lời tuyên bố D3 của người dùng): nếu Telegram chấp nhận thì việc tải lên biến mất hoàn toàn cho cả album. Hiển thị: dòng tiến độ tải lên dài (350 dòng cho 2,4 GB) đã được giảm (chỉ in khi tiến thêm 5% hoặc sau 30 giây; chưa xong thì không bao giờ hiện 100%).

Đã làm: cảnh báo khi ngân sách giảm (logger `tgmirror.core.pool`, CLI in `[cảnh báo] ...` ra stderr, bền với console không có tiếng Việt), tắt hachoir ồn, `--timeline` cho spike tải lên.

**Kết quả hai spike (2026-09-21, người dùng chạy).**

1. **Gửi album bằng mã file từ nguồn cấm lưu: Telegram từ chối.** `spike_reference.py ... 209041 209039 --album --yes-this-is-a-test-channel --yes-i-administer-this-channel` (hai video 321 MB và 175 MB, nguồn `noforwards=True`): `ChatForwardsRestrictedError: You can't forward messages from a protected chat`, từ `SendMultiMediaRequest`. Không sửa mã: `router(..., by_reference=not run.options.src_protected)` đã không bao giờ chọn `Strategy.REFERENCE` cho nguồn đó, và câu trả lời khép lại "cách kiểm nhanh hơn cả" ở trên: với nguồn cấm lưu, tải lên album là **không thể tránh**, nên tốc độ tải lên quyết định thời gian chạy. Chưa thử tin đơn (`SendMediaRequest`; chạy không có `--album`), nhiều khả năng cùng kết quả.
2. **`spike_throughput.py upload --mb 384 --inflight 8 --connections 1,2,4 --repeat 1 --timeline 10`** (dữ liệu rác, big file 768 part; một lượt mỗi cấu hình):

   | Kết nối | Kết quả | MB/s theo cửa sổ 10 giây |
   |---|---|---|
   | 1 | quá 90 s chưa xong (~283 MB đã đi, ~3,1 MB/s) | 7,7 2,4 3,4 1,9 2,7 2,9 3,0 2,5 1,8 |
   | 2 | 18,2 MB/s, xong sau ~21 s | 22,9 15,3 0,1 |
   | 4 | 27,6 MB/s, xong sau ~14 s | 29,1 9,3 |

   Đọc số: (i) **Không thấy Telegram hạn tốc độ tải lên bền vững** ở 2 và 4 kết nối: cả 384 MB (gấp 6 lần lượt spike trước) chạy hết ở 18–28 MB/s, cửa sổ đầu không hơn hẳn cửa sổ sau. Chỉ so trong ≤ 21 s: chưa loại được việc tụt sau vài phút hay vài GB (lần chạy thật 1,5 GB kéo dài vài phút). (ii) **Một kết nối là chỗ nghẽn**: 8 request trên 1 kết nối chỉ nhanh ở 10 giây đầu (7,7) rồi ~2–3 MB/s suốt 80 giây, bằng tốc độ một request một lúc (3,5 MB/s) và đúng dạng "nhanh lúc đầu rồi tụt" của các lần chạy thật. Lượt 1 kết nối chạy đầu tiên (có thể còn "lạnh") và mỗi cấu hình chỉ một lượt, nên đây là dấu hiệu chứ chưa phải kết luận (`--repeat 3`; lượt 64 MB trước đó cho 1 kết nối × 8 request 18,9, nhiễu 5,3–19,1). (iii) Số thật (~3 MB/s với pool, `upload_connections = 2`, ngân sách ≤ 4) thấp hơn nhiều so với 2 kết nối của spike (18 MB/s; 14,7 ở 4 request/64 MB), nên ngân sách 4 không đủ giải thích.

   **Giả thuyết còn lại (d), chưa kiểm**: kết nối tải lên thứ hai (hoặc cả hai) **không tạo được và pool âm thầm rơi về một kết nối, hay về kết nối chính**. Đọc mã: `_upload_senders` (`core/telethon_gateway.py`) nuốt lỗi của `_new_sender` (`except Exception: break`) và dùng kết nối chính nếu không tạo được cái nào, không in gì; ~3 MB/s trùng với tốc độ một kết nối. Spike tải xuống đã thấy server đóng kết nối ở một số cấu hình nhiều kết nối. Cách kiểm rẻ nhất: in cảnh báo (cùng đường `[cảnh báo]`) khi số kết nối tải lên tạo được ít hơn `upload_connections`, rồi chạy lại một video lớn. **Đã làm (2026-09-21)**: `_upload_senders` và `_download_sender` (logger `tgmirror.core.telethon_gateway`) nói ra, một lần cho mỗi gateway vì kết nối được giữ: `[cảnh báo] upload connections: only 1 of 2 could be made (ConnectionError: ...); uploads are slower than they should be` (hay "uploads share the main connection, which is much slower" khi không tạo được cái nào), và `download connection could not be made (...)`. Test ở `test_telethon_pool.py` (lỗi, một phần, không lỗi thì im lặng), hai đột biến mới trong `scripts/mutation_check.py` (đã chạy: **52/52 bắt được** sau khi sửa mẫu "FloodWait bị nuốt" của pool, mẫu đó không còn khớp mã sau bản sửa 429 nên lần chạy trước chỉ được 51/52). **Việc còn lại cho bạn**: chạy lại một **video đơn** ≥ 10 MB (không phải album: album không qua pool, xem lần chạy 17 dưới đây) bằng `--mode reupload`; thấy dòng đó thì (d) đúng và nguyên nhân là lỗi tạo kết nối (chữ trong ngoặc cho biết lỗi gì), không thấy thì (d) bị loại và nghi ngờ còn lại là Telegram hạn tốc độ sau vài phút.
   **Lần chạy thật thứ tư (2026-09-21, lần chạy 17, sau khi có cảnh báo kết nối).** `clone --hashtag '#SC' --mode reupload` từ nguồn cấm lưu (tuyên bố D3), đích forum. Tải xuống ổn định 16 → 26–29 MB/s (892 MB, rồi 277 MB ở 28,9 MB/s). **Không có cảnh báo nào** (pool hay kết nối), nhưng lần chạy này **không kiểm được (d)**: unit đầu là một **album** (dòng "Tải lên tin 188826" cộng 892,6 MB, hơn 892,4 MB của file 188827 đang tải xuống một chút: một tin nhỏ cộng video, tiến độ báo theo tin đầu của album; không có dòng tải xuống nào cho 188826 vì nhỏ hơn 8 MB), nên tải lên đi qua `send_file([...])` của Telethon trên **kết nối chính**, một request một lúc, và `_upload_senders` không được gọi. Tốc độ: **2,0 MB/s** ở 45–61 %, rồi **0,7–1,0 MB/s** (có lúc 215 KB/s) từ 65 % trở đi, khi không còn gì khác đang tải, tức tụt dần và tệ hơn cả 3,5 MB/s của "một request một lúc" ở spike. Suy ra từ tiến độ, không phải kết luận (dán lại chưa chắc đủ dòng).
   Hệ quả cho hàng việc: **đường album qua pool (việc 5) trở nên đáng làm** vì (1) và (i), nhưng làm sau khi (d) được xác nhận hay loại, kẻo dựng đường mới trên cùng một pool chậm.

#### Đẩy byte trước khi đăng — đã làm (2026-09-20)

Người dùng duyệt đổi crash-safety và pace ("đổi luôn bây giờ và test cùng với pool"). Đã có: `TelegramGateway.upload_prepared` và `Prepared.uploaded` (Telethon: `_pooled_upload`, `_Item.uploaded`; `FakeGateway`), `FloodGuard.transfer`/`check_cap`/`pace(cost, credit)`, `Limiter.check_cap` và `acquire(..., credit=)`, `Runner._mono` và thứ tự mới ở `_process` (kiểm cap → đẩy byte → `pace` với credit → cổng → write-ahead → đăng → commit). Cơ chế ở `04-state-checkpoint.md`, `05-chong-flood.md`. Kiểm bằng `tests/integration/test_runner_split.py` (thứ tự và số hàng `pending` tại từng thời điểm; kill lúc đẩy byte không để lại gì; credit theo thời gian đẩy, không rút ngắn nghỉ dài; cap kiểm trước khi đẩy; FloodWait khi đẩy lặp lại đẩy, khi đăng không đẩy lại; **cùng runner + store + `TelethonGateway` thật với pool trên client giả**), cùng các ca mới ở `test_limiter.py` và `test_telethon_pool.py`. Các test cũ không đổi (đồng hồ `mono` của Rig đứng yên nên không có credit ngẫu nhiên).

Các lựa chọn khi làm (không phải D1–D9):

- **Credit chỉ là thời gian đẩy byte** đo quanh `upload_prepared`, không phải mọi thời gian trôi qua từ lần đăng trước; bảo thủ hơn "≥ delay kể từ lần đăng trước", và tất định trong test.
- **Chỉ tin đơn là tài liệu/video ≥ 10 MB** (đường pool) đẩy trước; album, ảnh, file nhỏ, gửi bằng mã file và forward giữ thứ tự cũ. Album không tách được vì Telethon đổi media thành tham chiếu bằng một request ghi (`messages.uploadMedia`) nằm trong chính lời gọi đăng.
- **Ảnh bìa** của video vẫn được Telethon đẩy trong lời gọi đăng (nhỏ).
- **Không đọc lại tin theo lô** (mục thứ ba của bản đề xuất gốc): lợi ích thấp vì đọc đã chạy nền song song với gửi.

**Chưa kiểm chứng trên Telegram thật**: như mục pool, và thêm: đăng bằng `InputFileBig` đã đẩy từ trước không hết hạn khi chờ pace hoặc FloodWait dài (chưa biết file đã đẩy sống bao lâu trước khi Telegram bỏ); tốc độ thực tế của vòng đẩy → chờ → đăng so với trước.

#### Kế hoạch tăng tốc (đã chốt hướng, chưa làm)

Mô hình: **một bất biến duy nhất — số request Telegram đang bay ≤ `R`** (đọc lại tin, tải một part, đẩy một part, lệnh đăng đều chiếm một slot). Bộ lập lịch làm việc ở mức part, **unit cũ nhất được ưu tiên tuyệt đối** (kể cả giữa tải xuống của unit sau và tải lên của unit trước), nên đăng vẫn đúng thứ tự (D4), đĩa được giải phóng sớm, không deadlock, và phần đuôi file này liền mạch với phần đầu file kế. `Window` vẫn chặn đĩa. `R` tự chỉnh kiểu AIMD (FloodWait thì giảm một nửa, chạy êm thì tăng), có trần cứng cấu hình được (`[limits]`, mặc định khoảng 4 rồi đo). Số request theo dung lượng: ảnh nhỏ 1 xuống + 1 lên, file 10 MB+ đủ part để dùng hết `R`. Bộ lập lịch là phần thuần (kiểm bằng `PartTransport` giả có độ trễ ảo); adapter Telethon mỏng (theo tôi nhớ: tải tối đa 1 MiB/request và không vắt qua ranh giới 1 MiB, tải lên tối đa 512 KiB/part, file ≥ 10 MB dùng API big-file có thể gửi part song song — cần kiểm lại). Nhớ: với file nhỏ, nhiều file cùng lúc **không** làm nhanh hơn vì nhịp đăng (pace) chặn; lợi chủ yếu ở file 1–50 MB và file lớn.

Cập nhật sau spike 11 và 12 (2026-09-20): gửi bằng tham chiếu **chạy được** cho video và tệp, nên bước 4 dưới đây nên làm *trước* bộ lập lịch request: một lần chạy chỉ đổi caption trên nguồn không cấm lưu nội dung sẽ có tốc độ gần chiến lược A, không cần đĩa. Số đo tải lên cho `R` mặc định khoảng 8 request trên 2 kết nối.

**Bước 2 giải thích (tách đẩy byte khỏi đăng tin, pace theo deadline).** Hôm nay một unit lớn đi: `pace` (ngủ 2 s) → write-ahead (ghi `pending`) → `send_prepared` (đẩy byte **rồi** đăng tin) → `commit`. Đường truyền rảnh trong 2 s ngủ, và `pending` kéo dài suốt lúc đẩy byte. Đề xuất: đẩy byte trước (không tạo tin nào, Telegram bỏ file chưa được đăng), rồi mới chờ cho đủ ≥ 2 s **kể từ lần đăng trước**, rồi write-ahead, rồi chỉ *đăng* (một lời gọi ngắn), rồi `commit`. Đẩy byte của unit N chạy ngay sau khi đăng unit N−1, nên với file đẩy lâu hơn 2 s thì khoảng chờ đã hết khi byte xong và pace không tốn gì. Ví dụ video 55 MB (đẩy 2,5 s, đăng 0,3 s): hôm nay mỗi unit ≈ 2 + 2,8 = 4,8 s; sau đổi ≈ 2,8 s (nhanh hơn ~40%); video 500 MB (đẩy 25 s) chỉ bớt 2 s trên 27 s; ảnh nhỏ (đẩy 0,3 s) vẫn bị pace chặn như cũ. Nó **không làm tăng tần suất đăng tin quá mức đã cấu hình** (vẫn ≥ `min_delay` giữa hai lần đăng, đó mới là điều Telegram giới hạn), và có ba lợi phụ: cửa sổ `pending` chỉ còn một lời gọi đăng (crash lúc đang đẩy byte không để lại gì để reconcile), FloodWait khi đăng chỉ lặp lại lệnh đăng chứ không đẩy lại cả file, và có thể đẩy byte của unit kế trong lúc chờ. Giá phải trả: đổi thứ tự write-ahead/pace ở `runner._process`/`_send` và tách `send_prepared` thành hai lời gọi của gateway (chạm đúng vùng từng lỗi "video ra tệp"; `test_telethon_pin.py` đã giữ hành vi dựng media), kiểm `daily_cap` **trước** khi đẩy byte (không thì đẩy xong mới biết hết cap và phải đẩy lại), và nó chỉ đáng với file cỡ 10–100 MB. Người dùng đã duyệt làm ngay (2026-09-20); xem "Đẩy byte trước khi đăng — đã làm".

Thứ tự đề xuất, mỗi bước xin duyệt khi đổi hành vi:

1. Spike (người dùng chạy): `spike_count.py`, `spike_reference.py`, `spike_throughput.py`.
2. ~~**Tách "đẩy byte" khỏi "đăng tin"**~~ — **đã làm (2026-09-20, người dùng duyệt)**, xem "Đẩy byte trước khi đăng — đã làm"; ghi chú gốc: **Tách "đẩy byte" khỏi "đăng tin"** (`upload_file` rồi `send_file` với handle), **pace tính sau khi đẩy byte và trước lệnh đăng, theo deadline từ lần ghi trước**, `begin_batch` dời xuống ngay trước lệnh đăng; đọc lại tin theo lô ≤ 100 unit thay vì một request mỗi unit. *Đổi hành vi crash-safety và pace: cần người dùng duyệt riêng* (`04-state-checkpoint.md`, skill `checkpoint-state`, `flood-safety`).
3. ~~`RequestBudget` + bộ lập lịch part cho tải xuống, rồi tải lên (file ≥ 10 MB); trần kết nối; AIMD; giữ trần RAM~~ — **đã làm** (xem "Pool request — đã làm").
4. ~~Gửi bằng tham chiếu file cho tin chỉ đổi caption~~ — **đã làm** (xem trên); nguồn `noforwards` đã đo 2026-09-21: Telegram từ chối (`ChatForwardsRestrictedError`), nên nguồn đó luôn tải xuống rồi tải lên; album đã được spike.
5. Đường album mới (`InputMediaUploadedDocument` + `SendMultiMedia`) cho phần tải lên (không cần cho gửi bằng mã file); chờ đó album giữ đường `send_file([...])`. Sau spike 2026-09-21 (nguồn cấm lưu không gửi bằng mã file được; tải lên 2–4 kết nối không bị hạn tốc độ trong 384 MB) việc này đáng làm, sau khi kiểm giả thuyết (d) ở phần Pool request.
6. Relay theo part (part tải xuống xong thì đẩy lên ngay, không chờ cả file) — sau khi bộ lập lịch chạy ổn.
7. Đo `flood_log` thật (spike 6) để chỉnh `min_delay`, cap ngày và delay theo dung lượng (nút cổ chai của ảnh nhỏ là pace và cap, không phải tốc độ truyền).

Lưu ý cho phase 2 (đã áp dụng): schema đã có `jobs.src_kind`, `msg_map.src_topic_id`, `topic_map` (xem `04-state-checkpoint.md`) để phase 8 không cần migration. Đường code phase 1–7 vẫn viết với `kind` trong đầu, dù chỉ kiểm thử với broadcast.

## Việc cần xác minh sớm (spike, phase 0–1)

1. ~~Phiên bản Telethon cài đặt có tham số `drop_author` của `forward_messages` không?~~ **Xong 2026-09-19:** có. Telethon 1.45.0 `forward_messages(..., drop_author=, drop_media_captions=, as_album=)`. `pyproject.toml` đặt `telethon>=1.45` nên không cần fallback `ForwardMessagesRequest`.
2. ~~Forward một danh sách id có giữ nguyên album khi `drop_author=True` không?~~ **Xong 2026-09-20:** có, người dùng đã thử trên tài khoản thật (chưa đo riêng từng cỡ album).
3. `iter_messages(..., reverse=True, search=..., filter=...)` kết hợp `min_id` cho kết quả đúng thứ tự tăng dần? *(đọc mã Telethon 1.45: `min_id` thành `offset_id = min_id + 1`, `max_id` **loại trừ**; `search`/`filter` chuyển thành `messages.search` với `add_offset` âm, và `offset_date` trở thành `max_date` nên hỏng khi `reverse=True`, vì vậy ngày được đổi thành id bằng một lời gọi riêng. **Chưa thử trên Telegram thật**: chạy cùng một job với `--pushdown` và `--no-pushdown` rồi so số tin đã sao chép)*
4. Hành vi thực tế của quyền để xác định `can_post` và `is_admin` (broadcast, supergroup, group). **Phase 1** suy từ entity (`creator`, `admin_rights`, `banned_rights`, `default_banned_rights`, có xét `until_date`) thay vì `get_permissions`, vì gọi `get_permissions` cho từng dialog là một request mỗi kênh; đã khớp trên account thật với kênh broadcast, còn cần đối chiếu supergroup/group/forum trước khi tick.
5. Cách phát hiện `noforwards` đáng tin cậy (`Channel.noforwards`, và `Message.noforwards`). Phase 1 đọc `Channel.noforwards`/`Chat.noforwards` trong `channel_info`; `Message.noforwards` chưa dùng.
6. Số tin/lời gọi và delay nào chạy êm trên một account thử (đo, không đoán). *(Phase 4 đã có `flood_log` và `limiter_state` để đo; chưa có số liệu thật, các mặc định vẫn là điểm khởi đầu bảo thủ.)*
7. Với `drop_author=True`, Telegram xử lý thế nào các loại poll, quiz, location, contact, game, invoice khi forward (giữ, lỗi, hay đổi thành tin khác)? Quyết định chiến lược A có cần tiền kiểm theo loại tin hay không.
8. Quiz: admin/creator của nguồn có thấy đáp án đúng khi chưa trả lời không? (Telethon chỉ dựng lại được quiz khi có `results.results`.) Kiểm tra `poll.id`, `close_date`, `public_voters` có được chấp nhận khi gửi lại `InputMediaPoll`.
9. Forum: `ForwardMessagesRequest(top_msg_id=...)` có đưa tin vào đúng topic đích không; `CreateForumTopicRequest` (icon, General); `iter_messages` trả `reply_to.forum_topic`/`reply_to_top_id` đủ để định tuyến topic; `ToggleForumRequest` để tạo đích forum; danh sách topic (`GetForumTopicsRequest`) và topic đã đóng/ẩn.
10. ~~`messages.search` (`limit=1`, `min_id`/`max_id`, filter, `q`) có áp khoảng và phần lọc khi tính `count` không?~~ **Xong một phần 2026-09-20** (`scripts/spike_count.py`, kênh 72 tin, id mới nhất 114): **filter media được áp** (17 video = 17 khi đọc), **`min_id`/`max_id` bị bỏ qua** (luôn trả tổng 72; đọc thật sau id 57 là 34, tới id 57 là 38). Bản đầu của `count` vì vậy sai với mọi khoảng; đã sửa thành đếm theo *vị trí* (`offset_id_offset`, xem `01-kien-truc.md`): cần chạy lại spike để xác nhận mọi dòng "same". Chưa thử: `search` có từ khớp (kênh thử không có `#news`), filter ảnh có ảnh.
11. **Xong một phần 2026-09-20 (kênh không cấm lưu nội dung)**: gửi lại media **bằng tham chiếu file** — `send_file(dst, message.media, caption=...)` — được chấp nhận: video 56 MB (thời lượng 1232 s) và tệp `.srt` ra đúng loại, đúng dung lượng, không tải xuống hay tải lên. Thêm: video 184 MB (1504 s) cũng được. **Album cũng được** (2026-09-20, `send_file(dst, [media, ...])`): 6 video 115–490 MB ra 6 tin video đúng thời lượng, **cùng một nhóm album** ở đích. **Nguồn `noforwards` (D3), 2026-09-21: bị từ chối** — album 2 video (321 MB và 175 MB) với `--yes-i-administer-this-channel`: `ChatForwardsRestrictedError: You can't forward messages from a protected chat` do `SendMultiMediaRequest`; tin đơn chưa thử riêng (chạy không có `--album`). Chưa biết: `FILE_REFERENCE_EXPIRED` sau bao lâu, caption/entity/ảnh bìa có đúng không, thời gian gửi. Câu hỏi gốc: (`send_file(dst, message.media, caption=...)`, không tải, không tải lên) có được Telegram chấp nhận không, loại media (video, thời lượng, voice, sticker) có giữ nguyên không, album bằng danh sách media có giữ nhóm không, `FILE_REFERENCE_EXPIRED` xảy ra sau bao lâu? (`scripts/spike_reference.py`; ca nguồn `noforwards` theo D3.)
12. **Xong một phần 2026-09-20** (`scripts/spike_throughput.py`, tài khoản của người dùng, DC 5 là DC nhà, mỗi setting 3 lần lấy trung vị; đường truyền của máy thử khoảng 30–40 MB/s). **Tải xuống** (video 55 MB): Telethon `iter_download` 4,2 MB/s; request thô 1 MiB, 1 đang bay 6,9; 2 đang bay 15,6; 4 đang bay 28,6; 8 đang bay 34,3 (29–40), tất cả trên **một** kết nối. Thêm kết nối không giúp gì (2 request/2 kết nối 14,9 = 2 request/1 kết nối), và cấu hình 4 request/2 kết nối bị server đóng kết nối (`Server closed the connection`) khiến script treo (đã sửa: mỗi setting có timeout và báo lỗi). **Tải lên** (64 MB, big file, nhiễu lớn: có lần 4 request/1 kết nối chỉ 2,5 với một lượt 14,2): 1 request 3,5; 2 request 8,6; 8 request/1 kết nối 18,9 (5,3–19,1); 2 kết nối 2/4/8 request 9,5/14,7/15,5; **4 kết nối × 8 request 26,8 (22–29)**, cấu hình ổn định nhất. Không FloodWait ở cả hai chiều. Kết luận sơ bộ: **tải xuống chỉ cần một kết nối với 4–8 request đang bay** (nhanh gấp 5–8 lần cách hiện tại, khỏi cần `exportAuthorization` hay sender thứ hai cho DC nhà; DC khác dùng sender mượn của Telethon vẫn là một kết nối); **tải lên cần nhiều kết nối** (2–4) với khoảng 8 request đang bay, và việc tạo kết nối phải chịu lỗi được (lùi về ít kết nối hơn khi server đóng). Cách hiện tại (một request một lúc) là 4 MB/s xuống và 3,5 MB/s lên: video 55 MB mất khoảng 13 s + 16 s. **Tải xuống và tải lên cùng lúc** (`both`, tải 8 request/1 kết nối, lên 8 request/2 kết nối, 54 MB mỗi chiều, 3 lần): riêng lẻ tải xuống 29,7 và tải lên 21,2 (14,6–24,6); cùng lúc tải xuống vẫn 29,0 nhưng tải lên 16,2 (một lần **0**), và trong một lần có lỗi tầng truyền: `Invalid response buffer (HTTP code -100)`, `invalid checksum`, rồi **`HTTP code 429` ở tầng truyền — Telegram báo flood ngay trên kết nối, không phải `FloodWaitError`**; các lần sau vẫn chạy được. Đây là bằng chứng rằng **16 request đang bay trên 3 kết nối là quá tay**. Hệ quả cho thiết kế: trần tổng số request đang bay nên nhỏ (khoảng 8 chia cho cả hai chiều, khởi đầu 4 + 4) và tổng số kết nối ≤ 3 (1 xuống + 2 lên); `InvalidBufferError` mã 429 phải được ánh xạ thành tín hiệu flood (lùi một nửa, ghi `flood_log` với loại riêng như `transport_429`, chờ rồi tăng lại), các `InvalidBufferError`/`Server closed the connection` khác là lỗi kết nối để kết nối lại và lùi; gửi cùng một part lại sau lỗi. Câu hỏi gốc: một kết nối một request so với K request đang bay trên một kết nối, so với K kết nối; FloodWait có xuất hiện khi K lớn không; giới hạn tải xuống của tài khoản không Premium? (`scripts/spike_throughput.py`.) Quyết định cần bao nhiêu kết nối và `R` mặc định.
13. Giới hạn dung lượng upload của tài khoản (2 GB hay 4 GB Premium; theo tôi nhớ có trong cấu hình phía server `upload_max_fileparts_*`): analyze cần báo file vượt giới hạn trước khi chạy, không đợi tới lúc lỗi.

## Câu hỏi mở

Hiện không có. Các câu hỏi phát sinh trong lúc thiết kế đều đã được trả lời (bảng dưới).

## Câu hỏi đã trả lời (2026-09-19)

| Câu hỏi | Trả lời | Nơi ghi |
|---|---|---|
| Nguồn là supergroup/group hay chỉ broadcast? | Tất cả: broadcast, supergroup, group, forum | `00-tong-quan.md` (mục tiêu), `01-kien-truc.md` |
| Forum: ánh xạ topic → topic ở đích? | Có | `01-kien-truc.md`, `04-state-checkpoint.md` (`topic_map`) |
| Chế độ chạy nền (daemon)? | Không, không lên lịch. Chỉ chạy tay: `clone`/`run` chạy ngay trong terminal (lúc đó gọi là `sync`, nay là chạy lại) | `00-tong-quan.md` (không phải mục tiêu) |
| Nhiều account? | Không, một account | `00-tong-quan.md` (không phải mục tiêu) |
| Tên PyPI/GitHub còn trống? | Không (`twingram` đã bị chiếm). Chọn **tgmirror** | `00-tong-quan.md` (Tên dự án) |
| Poll/quiz/location/contact/invoice/game khi copy? | Location/contact: giữ. Poll: giữ nhưng mất vote (opt-in `--reset-polls`). Quiz: giữ nếu đã trả lời. Game/invoice: bỏ + cảnh báo (`--ignore-unsupported`) | `01-kien-truc.md`, `02-cli-ux.md`, `04-state-checkpoint.md` |
| Đích có sẵn của nguồn group/forum bắt buộc cùng loại (forum → forum)? | Có | `01-kien-truc.md` |
| Thêm tiền tố tên người gửi khi copy từ group (chiến lược B)? | Không cần | `01-kien-truc.md` (Hạn chế khi copy từ group) |
| Cờ gửi tin text thay thế cho tin bị bỏ? | Có: `--placeholder` | `01-kien-truc.md`, `02-cli-ux.md` |

## Nhật ký quyết định

Khi đổi một quyết định D1..D9 trong `00-tong-quan.md`, ghi ngày và lý do ở đây.

- 2026-09-20: Đợt tăng tốc và tiến độ (Phase 6b, xem "Phase 6b — ghi chú"). Không đổi D1–D9. **Đảo lại một lựa chọn của phase 6**: "chỉ pipeline tải trước, không nhiều kết nối kiểu FastTelethon" (ghi cùng ngày) — người dùng nay chọn có nhiều request/kết nối trong một ngân sách chung, vì đã hiểu chi phí (API private của Telethon, khó kiểm bằng fake, chờ số đo); skill `telethon-engine` sửa theo. Người dùng chốt: analyze chỉ đếm (mặc định), không IPC, nhiều file cùng lúc miễn không vượt trần request (dùng theo dung lượng), tải nhanh hơn gửi phải có trần cứng, cho phép spike. Đợt này làm phần rủi ro thấp (đếm, tiến độ, trần tải trước, script spike); đổi pace/write-ahead và bộ lập lịch request chờ duyệt riêng và số đo.
- 2026-09-20: **Đổi D3.** Trước: nguồn `noforwards` mà tài khoản không phải admin thì từ chối bất kể cờ; admin thì `--mode reupload` kèm xác nhận. Sau: mặc định vẫn tôn trọng `noforwards`, nhưng user có thể tự tuyên bố và **chịu hoàn toàn trách nhiệm** bằng `--mode reupload --yes-i-administer-this-channel`, và cờ này cho qua **cả tài khoản không phải admin** (lý do của user: chủ kênh thường có nhiều tài khoản, tài khoản đang chạy tgmirror có thể không phải cái đứng tên admin). Đi kèm: mỗi lần chạy dựa trên lời tuyên bố in `warn.responsibility`; `--yes` vẫn không thay được cờ; câu hỏi tương tác vẫn chỉ cho tài khoản là admin; tgmirror không kiểm tra được quyền sở hữu và nói rõ điều đó; lời tuyên bố (`RunOptions.protected_ack`) theo cả cặp, `run`/`retry` dùng lại. CLAUDE.md luật 5, `docs/00`, `01`, `02` và các skill đã sửa theo. Người dùng yêu cầu đổi (không phải suy diễn của Claude).

- 2026-09-20: Phase 6 xong (chiến lược B: reupload, `--caption`, tin đặc thù, tải trước). Không đổi D1–D9; D3 được cài đặt đúng chữ ("kèm xác nhận"): xác nhận là câu hỏi hoặc `--yes-i-administer-this-channel`, `--yes` không thay được. Người dùng chọn phạm vi (đủ theo lộ trình) và chọn pipeline tải trước thay cho uploader nhiều kết nối, trước khi viết mã. Đổi so với thiết kế cũ: bỏ `core/uploader.py` và `[limits] upload_concurrency` (thay bằng `prefetch`, `tmp_budget_mb`); tin không hỗ trợ dừng lần chạy thay vì hỏi ở bước xem trước; chưa có delay theo dung lượng. Các lựa chọn nhỏ và hai lỗi đã sửa ghi ở "Phase 6 — ghi chú".

- 2026-09-20: Phase 5 xong (`retry`, `status`). Không đổi D1–D9, không đổi schema. Các lựa chọn nhỏ và một lỗi mất-tin-lỗi đã sửa ghi ở "Phase 5 — ghi chú". Người dùng yêu cầu bắt đầu phase 5 theo lộ trình; các chi tiết thiết kế (retry là một lần chạy, `status` chỉ đọc DB, tin đã xóa thành `skipped`) chưa được duyệt riêng: đổi được nếu người dùng muốn khác.
- 2026-09-20: Thêm `--fresh` (xem "`--fresh` — làm lại từ đầu"): cờ trên `clone`/`run`, chỉ quên tiến độ, có câu hỏi hàng rào khi đích có thể bị trùng; không phải D1–D9, người dùng đã duyệt kế hoạch trước khi viết mã.
- 2026-09-20: Bỏ khái niệm job (xem "Tái thiết luồng job"). `clone` chạy ngay trong foreground; chỉ lưu nhật ký các lần chạy (`runs`) cộng một điểm kiểm tra ẩn theo cặp (`mirrors`); chạy lại cùng cặp là delta, filter được nhớ; `run`/`pause`/`stop` giữ nhưng nhắm vào lần chạy; pause giữ tại chỗ; phím `p`/`r`/`q`. D5 (một file SQLite) giữ nguyên, chỉ đổi lý do ("`tgmirror jobs` đơn giản" → "`tgmirror history` đơn giản"). Không viết migration từ `jobs` (dự án đang phát triển, người dùng chọn xóa DB cũ). Đảo ngược quyết định "mỗi cặp một job, cặp đã có job thì mã 2" (commit 9ed8509): cùng một cặp giờ là trường hợp delta. Người dùng đã duyệt kế hoạch trước khi viết mã.

- 2026-09-19: Đổi tên dự án `twingram` → **tgmirror** (package `tgmirror`, lệnh `tgmirror`, DB `tgmirror.db`, biến môi trường `TGMIRROR_API_ID`/`TGMIRROR_API_HASH`), vì `twingram` đã bị chiếm trên PyPI/GitHub. Lệnh `twin` bỏ hẳn, không giữ alias.
- 2026-09-19: Mở rộng phạm vi v1 từ "chỉ broadcast" sang mọi loại nguồn (broadcast, supergroup, group, forum) kèm ánh xạ topic; thành phase 8. Không phải D1–D9 nhưng đổi danh sách "không phải mục tiêu". Ngược lại chốt **không** làm daemon và **không** làm multi-account.
- 2026-09-19: Chốt: đích có sẵn phải cùng loại nguồn; không thêm tên người gửi; có cờ `--placeholder` gửi tin text thay thế cho tin bị bỏ vì không hỗ trợ.
- 2026-09-19: Chốt xử lý tin đặc thù cho chiến lược B: poll/quiz/location/contact giữ (poll mất vote, opt-in), game/invoice bỏ + cảnh báo. Thêm trạng thái `msg_map.status='skipped'`. Đoạn code mẫu trong ghi chú nguồn (`PollAnswerSyntax`, tự lắp `InputMediaPoll`) **không dùng**: `PollAnswerSyntax` không tồn tại trong Telethon 1.45; dùng `send_message(file=message.media)`.

- 2026-09-20: Phase 4 xong. Không đổi D1–D9 (D6 vẫn là `flood_sleep_threshold=0`, mọi FloodWait vào limiter của mình). Thêm khóa `[limits] read_delay` và cờ `run --wait` (`--wait` đã có trong thiết kế ở `05-chong-flood.md`); các lựa chọn nhỏ ghi ở "Phase 4 — ghi chú".
- 2026-09-20: Phase 3 xong. Không đổi D1–D9. Thêm dependency `pyyaml` và `regex`. Các lựa chọn nhỏ ghi ở "Phase 3 — ghi chú" và `03-filters.md`: `date` nửa mở, thiếu thuộc tính thì predicate sai, không đẩy `contains`, `--refilter` là cách đổi filter.
- 2026-09-20: Phase 2 xong. Không đổi D1–D9. Các lựa chọn nhỏ ghi ở "Phase 2 — ghi chú": limiter tạm thời, FloodWait dừng job thay vì chờ (cho tới phase 4), một job cho mỗi cặp nguồn/đích, `run` chạy được với job `done`, `last_message_id` vào protocol.
- 2026-09-19: Thêm skill `doc-sync` và luật "mỗi task đều xét cập nhật docs/skills" (CLAUDE.md, luật 9).
- 2026-09-19: Bản thiết kế đầu tiên. D3 đổi so với brainstorm ban đầu (brainstorm cho phép reupload cho mọi kênh `noforwards`; bản này tôn trọng cài đặt bảo vệ nội dung của chủ kênh).
